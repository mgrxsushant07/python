# Area of a Rectangle
# Write a program to calculate the area of a rectangle. Use the formula: Area = length × width

length=float(input("Enter length :"))
width=float(input('Enter width :'))

#by using formula :Area = length × width

area=length * width

print(f'The area of rectange is {area}..')